# 🎫 PassCard - Custom Apple Wallet Passes

Полноценное решение для создания кастомных пассов Apple Wallet (.pkpass) с iOS-приложением и сервером для подписи.

## 📋 Содержание

- [Обзор](#обзор)
- [Архитектура](#архитектура)
- [Требования](#требования)
- [Быстрый старт](#быстрый-старт)
- [Настройка сертификатов Apple](#настройка-сертификатов-apple)
- [Запуск сервера](#запуск-сервера)
- [Сборка iOS приложения](#сборка-ios-приложения)
- [API документация](#api-документация)
- [Структура pass.json](#структура-passjson)
- [Динамические обновления](#динамические-обновления)
- [Безопасность](#безопасность)
- [Troubleshooting](#troubleshooting)

---

## 🎯 Обзор

PassCard позволяет создавать кастомные билеты, купоны и карты лояльности для Apple Wallet:

### Возможности

- ✅ Создание билетов на события (eventTicket)
- ✅ Посадочные талоны (boardingPass)
- ✅ Купоны и скидки (coupon)
- ✅ Карты магазинов (storeCard)
- ✅ Универсальные карты (generic)
- ✅ Кастомные цвета и изображения
- ✅ QR-коды, PDF417, Aztec, Code128
- ✅ Добавление в Apple Wallet
- ✅ Динамические обновления (опционально)

---

## 🏗 Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                       iOS App (SwiftUI)                      │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────┐ │
│  │ CreatePass│  │ PassPreview│  │AddToWallet│  │  Settings │ │
│  └─────┬─────┘  └─────┬─────┘  └─────┬─────┘  └───────────┘ │
│        │              │              │                       │
│        └──────────────┴──────────────┘                       │
│                       │                                      │
│              ┌────────┴────────┐                             │
│              │  PassAPIService │                             │
│              └────────┬────────┘                             │
└───────────────────────┼─────────────────────────────────────┘
                        │ HTTPS
                        ▼
┌─────────────────────────────────────────────────────────────┐
│                    Node.js Server                            │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐               │
│  │  Express  │──│PassGenerat│──│   Sign    │               │
│  │  Routes   │  │    or     │  │  (PKCS#7) │               │
│  └───────────┘  └───────────┘  └───────────┘               │
│                       │                                      │
│              ┌────────┴────────┐                             │
│              │  Certificates   │                             │
│              │  (Apple + WWDR) │                             │
│              └─────────────────┘                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📱 Требования

### iOS App
- Xcode 15.0+
- iOS 16.0+
- Swift 5.9+
- Apple Developer Account

### Server
- Node.js 18+
- npm или yarn
- Apple Developer Certificate (Pass Type ID)

---

## 🚀 Быстрый старт

### 1. Клонирование и установка

```bash
# Перейдите в директорию проекта
cd /path/to/PassCard

# Установите зависимости сервера
cd Server
npm install
```

### 2. Настройка сертификатов (см. раздел ниже)

### 3. Запуск сервера

```bash
cd Server
npm run dev
```

### 4. Откройте iOS проект в Xcode

```bash
open PassCard.xcodeproj
```

### 5. Измените URL сервера (для реального устройства)

В настройках приложения укажите URL вашего сервера (ngrok или публичный).

---

## 🔐 Настройка сертификатов Apple

### Шаг 1: Создание Pass Type ID

1. Войдите в [Apple Developer Portal](https://developer.apple.com/account)
2. Перейдите в **Certificates, Identifiers & Profiles**
3. В разделе **Identifiers** выберите **Pass Type IDs**
4. Нажмите **+** для создания нового
5. Введите описание и идентификатор (например: `pass.com.yourcompany.passcard`)
6. Сохраните

### Шаг 2: Создание сертификата

1. Выберите созданный Pass Type ID
2. Нажмите **Create Certificate**
3. Следуйте инструкциям для создания CSR (Certificate Signing Request):
   ```bash
   # На Mac откройте Keychain Access
   # Keychain Access → Certificate Assistant → Request a Certificate From a Certificate Authority
   # Выберите "Saved to disk"
   ```
4. Загрузите CSR и скачайте сертификат (.cer)
5. Дважды кликните на .cer для установки в Keychain

### Шаг 3: Экспорт сертификатов

```bash
# 1. Откройте Keychain Access
# 2. Найдите "Pass Type ID: pass.com.yourcompany.passcard"
# 3. Экспортируйте в .p12 (с паролем)

# Конвертация в PEM формат
cd Server/certificates

# Извлечение сертификата
openssl pkcs12 -in Certificates.p12 -clcerts -nokeys -out signerCert.pem

# Извлечение приватного ключа
openssl pkcs12 -in Certificates.p12 -nocerts -out signerKey.pem

# Удаление пароля с ключа (опционально, для разработки)
openssl rsa -in signerKey.pem -out signerKey.pem
```

### Шаг 4: Скачивание WWDR сертификата

```bash
# Apple Worldwide Developer Relations Certification Authority
# Скачайте с https://www.apple.com/certificateauthority/

# Для iOS 13+, используйте G4:
curl -o WWDR.pem https://www.apple.com/certificateauthority/AppleWWDRCAG4.cer

# Конвертация в PEM
openssl x509 -inform DER -in AppleWWDRCAG4.cer -out WWDR.pem
```

### Шаг 5: Структура директории certificates

```
Server/
└── certificates/
    ├── signerCert.pem    # Ваш сертификат Pass Type ID
    ├── signerKey.pem     # Приватный ключ
    └── WWDR.pem          # Apple WWDR сертификат
```

### Шаг 6: Настройка переменных окружения

```bash
# Создайте .env файл в Server/
cat > Server/.env << EOF
PASS_TYPE_ID=pass.com.yourcompany.passcard
TEAM_ID=XXXXXXXXXX
PORT=3000
PASS_KEY_PASSWORD=your_key_password_if_any
EOF
```

---

## 🖥 Запуск сервера

### Разработка

```bash
cd Server
npm run dev
```

### Продакшен

```bash
cd Server
npm start
```

### С ngrok (для тестирования с реального устройства)

```bash
# В одном терминале
cd Server && npm run dev

# В другом терминале
ngrok http 3000
```

Скопируйте ngrok URL и вставьте в настройки iOS приложения.

---

## 📱 Сборка iOS приложения

### 1. Откройте проект

```bash
open PassCard.xcodeproj
```

### 2. Настройте Signing & Capabilities

- Выберите свой Team
- Измените Bundle Identifier
- Добавьте capability: **Wallet**

### 3. Запустите на устройстве

⚠️ **Важно**: Apple Wallet работает только на реальных устройствах, не на симуляторе!

---

## 📡 API документация

### Base URL
```
http://localhost:3000/api/passes
```

### Endpoints

#### POST /create
Создание нового пасса.

**Request:**
```json
{
  "ticket": {
    "eventName": "Концерт Example",
    "eventDescription": "Юбилейный концерт",
    "eventDate": "2024-12-31T19:00:00Z",
    "eventTime": "2024-12-31T19:00:00Z",
    "venueName": "Крокус Сити Холл",
    "venueAddress": "Москва, МКАД 65-66 км",
    "seatInfo": "Сектор A, Ряд 5, Место 12",
    "ticketType": "eventTicket",
    "backgroundColor": "#1E1E2E",
    "foregroundColor": "#FFFFFF",
    "labelColor": "#A6ADC8",
    "organizationName": "Concert Agency",
    "barcodeMessage": "TICKET-2024-001234",
    "barcodeFormat": "PKBarcodeFormatQR"
  },
  "logoImageBase64": "iVBORw0KGgo...",
  "iconImageBase64": "iVBORw0KGgo...",
  "backgroundImageBase64": "iVBORw0KGgo..."
}
```

**Response:**
```
Content-Type: application/vnd.apple.pkpass
X-Serial-Number: PASS-1704067200000-ABC12345
[binary .pkpass data]
```

#### GET /:serialNumber
Получение существующего пасса.

#### DELETE /:serialNumber
Удаление пасса.

#### GET /
Список всех пассов.

---

## 📋 Структура pass.json

```json
{
  "formatVersion": 1,
  "passTypeIdentifier": "pass.com.yourcompany.passcard",
  "teamIdentifier": "XXXXXXXXXX",
  "serialNumber": "PASS-1704067200000-ABC12345",
  "organizationName": "Concert Agency",
  "description": "Концерт Example",
  
  "backgroundColor": "rgb(30, 30, 46)",
  "foregroundColor": "rgb(255, 255, 255)",
  "labelColor": "rgb(166, 173, 200)",
  
  "barcodes": [{
    "format": "PKBarcodeFormatQR",
    "message": "TICKET-2024-001234",
    "messageEncoding": "iso-8859-1"
  }],
  
  "relevantDate": "2024-12-31T19:00:00+03:00",
  
  "eventTicket": {
    "headerFields": [{
      "key": "time",
      "label": "ВРЕМЯ",
      "value": "19:00"
    }],
    "primaryFields": [{
      "key": "event",
      "label": "СОБЫТИЕ",
      "value": "Концерт Example"
    }],
    "secondaryFields": [
      {"key": "venue", "label": "МЕСТО", "value": "Крокус Сити Холл"},
      {"key": "date", "label": "ДАТА", "value": "31 декабря 2024"}
    ],
    "auxiliaryFields": [{
      "key": "seat",
      "label": "МЕСТО",
      "value": "Сектор A, Ряд 5, Место 12"
    }],
    "backFields": [
      {"key": "organization", "label": "Организатор", "value": "Concert Agency"},
      {"key": "address", "label": "Адрес", "value": "Москва, МКАД 65-66 км"}
    ]
  }
}
```

---

## 🔄 Динамические обновления

Для поддержки автоматических обновлений пассов:

### 1. Настройте webServiceURL

В `Server/src/config.js`:
```javascript
const PASS_CONFIG = {
  webServiceURL: 'https://your-server.com/api/passes',
  // ...
};
```

### 2. Реализуйте Web Service API

Сервер уже содержит базовые endpoints:
- `POST /v1/devices/:deviceId/registrations/:passTypeId/:serialNumber`
- `DELETE /v1/devices/:deviceId/registrations/:passTypeId/:serialNumber`
- `GET /v1/devices/:deviceId/registrations/:passTypeId`
- `GET /v1/passes/:passTypeId/:serialNumber`
- `POST /v1/log`

### 3. Отправка Push-уведомлений

Для обновлений используйте Apple Push Notification service (APNs):
```javascript
// Пример с библиотекой apn
const apn = require('apn');

const provider = new apn.Provider({
  cert: 'path/to/cert.pem',
  key: 'path/to/key.pem'
});

// Отправка пустого уведомления для обновления пасса
const notification = new apn.Notification();
notification.topic = 'pass.com.yourcompany.passcard';

provider.send(notification, pushToken);
```

---

## 🔒 Безопасность

### Рекомендации

1. **Никогда не храните сертификаты в iOS приложении**
   - Все операции подписи выполняются на сервере

2. **Используйте HTTPS в продакшене**
   ```javascript
   const https = require('https');
   const fs = require('fs');
   
   const options = {
     key: fs.readFileSync('server-key.pem'),
     cert: fs.readFileSync('server-cert.pem')
   };
   
   https.createServer(options, app).listen(443);
   ```

3. **Валидация данных**
   - Проверяйте уникальность serialNumber
   - Валидируйте входные данные

4. **Rate Limiting**
   ```javascript
   const rateLimit = require('express-rate-limit');
   
   app.use('/api/', rateLimit({
     windowMs: 15 * 60 * 1000,
     max: 100
   }));
   ```

5. **Переменные окружения**
   - Храните секреты в .env файле
   - Никогда не коммитьте .env в git

---

## ❓ Troubleshooting

### Ошибка "Unable to read pass"
- Проверьте правильность сертификатов
- Убедитесь, что WWDR сертификат актуальный
- Проверьте passTypeIdentifier и teamIdentifier

### Пасс не добавляется в Wallet
- Работает только на реальном устройстве
- Проверьте, что Bundle ID приложения совпадает
- Проверьте capability Wallet в Xcode

### Сервер не отвечает на реальном устройстве
- Используйте ngrok или публичный сервер
- Проверьте URL в настройках приложения
- localhost не работает на реальном устройстве

### Ошибка подписи
- Проверьте формат сертификатов (PEM)
- Убедитесь, что ключ без пароля или пароль указан
- Проверьте срок действия сертификатов

---

## 📁 Структура проекта

```
PassCard/
├── PassCard/                    # iOS приложение
│   ├── PassCardApp.swift        # Entry point
│   ├── ContentView.swift        # Главный экран
│   ├── Models/
│   │   ├── PassTicket.swift     # Модель билета
│   │   └── SavedPass.swift      # Сохранённый пасс
│   ├── Views/
│   │   ├── CreatePassView.swift # Форма создания
│   │   ├── PassPreviewView.swift# Предпросмотр
│   │   ├── AddToWalletView.swift# Добавление в Wallet
│   │   ├── PassDetailView.swift # Детали пасса
│   │   └── SettingsView.swift   # Настройки
│   ├── Services/
│   │   ├── PassAPIService.swift # API клиент
│   │   └── WalletService.swift  # Работа с Wallet
│   ├── Utilities/
│   │   ├── QRCodeGenerator.swift# Генератор кодов
│   │   ├── ColorExtensions.swift# Работа с цветами
│   │   └── ImageExtensions.swift# Работа с изображениями
│   └── Assets.xcassets/
│
├── Server/                      # Node.js сервер
│   ├── package.json
│   ├── src/
│   │   ├── index.js            # Entry point
│   │   ├── config.js           # Конфигурация
│   │   ├── routes/
│   │   │   └── passes.js       # API routes
│   │   └── services/
│   │       └── PassGenerator.js # Генератор пассов
│   ├── certificates/           # Сертификаты Apple
│   │   ├── signerCert.pem
│   │   ├── signerKey.pem
│   │   └── WWDR.pem
│   ├── generated/              # Сгенерированные пассы
│   └── templates/              # Шаблоны изображений
│       ├── icon.png
│       └── logo.png
│
├── PassCard.xcodeproj/
└── README.md
```

---

## 📄 Лицензия

MIT License

---

## 🙏 Благодарности

- [Apple PassKit Documentation](https://developer.apple.com/documentation/passkit)
- [node-forge](https://github.com/digitalbazaar/forge)
- [archiver](https://github.com/archiverjs/node-archiver)
