# PassCard Flask Server

Flask сервер для генерации Apple Wallet passes (.pkpass).

## 🔄 Keep-Alive (сервер не засыпает)

На бесплатном плане Render сервис засыпает после 15 минут неактивности.
Встроенный keep-alive механизм автоматически пингует сервер каждые 14 минут.

**Для активации:** установите переменную `RENDER_EXTERNAL_URL` (Render делает это автоматически)
или укажите `WEB_SERVICE_URL` вручную.

Альтернативы:
- **Starter план** ($7/мес) — сервер никогда не засыпает
- **UptimeRobot** (бесплатно) — внешний мониторинг с пингом каждые 5 минут

## Деплой на Render

### 1. Создайте аккаунт на Render
Перейдите на https://render.com и зарегистрируйтесь.

### 2. Создайте новый Web Service
1. Нажмите **New** → **Web Service**
2. Подключите ваш GitHub репозиторий
3. Укажите путь к папке: `FlaskServer`

### 3. Настройки сервиса
- **Name:** passcard-server
- **Runtime:** Python 3
- **Build Command:** `pip install -r requirements.txt`
- **Start Command:** `gunicorn app:app --bind 0.0.0.0:$PORT`

### 4. Environment Variables
Добавьте переменные окружения:
- `PASS_TYPE_ID` = `pass.com.needsomevibe.passcard`
- `TEAM_ID` = `XFL8CQ52JZ`
- `ORG_NAME` = `PassCard`
- `PASS_KEY_PASSWORD` = (если ключ защищён паролем)

### 5. Сертификаты
⚠️ **ВАЖНО:** Для работы на Render нужно добавить сертификаты как файлы.

Вариант 1: Через Base64 (рекомендуется)
```bash
# Закодируйте сертификаты в base64
base64 -i certificates/signerCert.pem
base64 -i certificates/signerKey.pem
base64 -i certificates/WWDR.pem
```
Добавьте их как переменные окружения:
- `SIGNER_CERT_BASE64`
- `SIGNER_KEY_BASE64`
- `WWDR_CERT_BASE64`

Вариант 2: Добавьте файлы напрямую в репозиторий (менее безопасно).

## Локальный запуск

```bash
# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt

# Запустите сервер
python app.py
```

Сервер запустится на http://localhost:3000

## API Endpoints

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/health` | Health check |
| POST | `/api/passes/create` | Создание пасса |
| GET | `/api/passes/<serial>` | Получение пасса |
| PUT | `/api/passes/<serial>` | Обновление пасса |
| DELETE | `/api/passes/<serial>` | Удаление пасса |
| GET | `/api/passes` | Список пассов |

## Структура проекта

```
FlaskServer/
├── app.py              # Основное приложение
├── pass_generator.py   # Генератор пассов
├── requirements.txt    # Зависимости
├── render.yaml         # Конфигурация Render
├── certificates/       # Сертификаты Apple
│   ├── signerCert.pem
│   ├── signerKey.pem
│   └── WWDR.pem
├── templates/          # Шаблоны изображений
└── generated/          # Сгенерированные пассы
```
